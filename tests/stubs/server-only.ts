/**
 * Substitut du paquet `server-only` pour l'environnement de test.
 * En production, ce paquet garantit qu'un module serveur ne peut jamais être
 * inclus dans un bundle client ; hors Next.js, il doit être neutralisé.
 */
export {};
